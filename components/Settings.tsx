import * as React from "react";
import { Text } from "react-native";

interface SettingsProps {}

const Settings: React.FunctionComponent<SettingsProps> = (props) => {
    return <Text>Settings sak</Text>;
};

export default Settings;
